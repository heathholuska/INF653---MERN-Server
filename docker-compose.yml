version: '3.8'

services:
  api:
    build:
    ports:
      - "3500:3500"
    environment:
      - DATABASE_URI=mongodb://db:27017/mernStack
      - PORT=3500
    depends_on:
      -db
  db:
    image: mongo:latest
    ports:
      - "27017:27017"
      - mongo-data:/data/db
volumes:
  mongo-data: